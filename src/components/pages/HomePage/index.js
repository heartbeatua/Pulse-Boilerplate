import React from 'react';
import { BaseTemplate, Header } from 'components';

const Home = () => (
  <BaseTemplate header={<Header />}>
    <div>
      <p>home page</p>
    </div>
  </BaseTemplate>
);

export default Home;
