import Box from '../../src/components/atoms/Box';
import PropsTable from '../../src/components/molecules/PropsTable';

# Box

<PropsTable component={Box} />

The __Box__ is an element, which can be modify by yourself as you need. Its like a "div" on steroids.

Examples:

```.mdx
<Box bg="cta" color="white" maxWidth="50%" p="20px">I am a box.</Box>
```
